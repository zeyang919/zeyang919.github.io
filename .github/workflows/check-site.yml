name: Check static site

on: [push, pull_request]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node --test scripts/build.test.cjs
      - run: node scripts/build.cjs --check
      - run: node --check static/js/scripts.js
      - run: node --check static/js/navigation.js
